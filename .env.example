# LLM provider configuration.
# Leave GEMINI_API_KEY empty to run with the built-in mock LLM client
# (no API calls, no key needed, deterministic placeholder output).
LLM_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
